interface AdMarquee3DProps {
  darkMode?: boolean;
}

/** Placeholder — 3D ad carousel shows only when ads are configured in admin */
export function AdMarquee3D(_props: AdMarquee3DProps) {
  return null;
}
